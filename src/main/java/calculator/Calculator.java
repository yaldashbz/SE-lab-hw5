package calculator;
import java.lang.Math;

public class Calculator {
    public double sqrtDivide(int a, int b) {
        double tmp = (double) a / (double) b;
        return Math.sqrt(tmp);
    }
}
